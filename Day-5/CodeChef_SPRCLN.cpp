#include <bits/stdc++.h>
using namespace std;

int main() {
    // your code goes here
    
    int x, y;
    cin >> x >> y;  // Read two integers from input
    
    // Compute the total value as:
    // (x * 30) + (y * 60)
    // Meaning of this expression depends on the problem context — 
    // possibly converting x and y into weighted totals (like cost, time, or points)
    
    cout << (x * 30 + y * 60) << endl;  // Output the computed result
    
    return 0;  // End of program
}
